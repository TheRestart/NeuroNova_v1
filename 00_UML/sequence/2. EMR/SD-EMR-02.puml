@startuml
title SD-EMR-02 환자 기본 정보 조회
' UC Trace: UC2-2-1

actor Doctor as d
participant "CDSS UI" as ui
participant "Django CDSS\nEMR Proxy (UC2)" as emr
participant "OpenEMR API" as openemr

group 환자 기본 정보 조회
  d -> ui : 환자 선택
  ui -> emr : GET /patients/{id}
  emr -> emr : cache check
  alt cache miss
    emr -> openemr : REST /patients/{id}
    openemr --> emr : patient detail
    emr -> emr : cache update
  end
  emr --> ui : patient profile DTO
  ui --> d : 기본 정보 표시
end group
@enduml
