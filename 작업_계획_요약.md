# 작업 계획 요약

**최종 업데이트**: 2026-01-05
**현재 상태**: brain_tumor_dev 코드 통합 완료 (Backend), React 통합 대기 중

---

## 최근 완료 작업 (2026-01-05)

### brain_tumor_dev 통합 (Backend Django)

- [x] RBAC 앱 생성 및 통합 (accounts → rbac 변경)
- [x] Menus 앱 생성 및 통합 (권한 기반 메뉴 관리)
- [x] WebSocket 설정 (실시간 권한 알림)
- [x] Django 설정 업데이트 (INSTALLED_APPS, URLs)
- [x] 마이그레이션 생성 완료

### 인프라 및 프론트엔드 기반 (2026-01-05)

- [x] **Docker 아키텍처 재구성**: Celery Worker 분리 (Default, AI, FHIR) 및 큐 라우팅 적용
- [x] **로컬 개발 환경 최적화**: Docker MySQL 대신 Local MySQL(3306) 사용 정책 적용
- [x] **TSX 프론트엔드 기반**: Axios 인터셉터(JWT), App.tsx 라우팅, RBAC 시딩 스크립트 고도화

### 다음 단계

- [ ] **RBAC 데이터 초기화**: `seed_rbac_data` 스크립트 실행 (로컬 DB 대상)
- [ ] **React 코드 통합**: brain_tumor_dev의 권한 기반 라우팅 및 메뉴 렌더링 코드 통합
- [ ] **통합 테스트**: Frontend-Backend API 통신 검증

---

## 기존 작업 계획

#### 1차: 데이터베이스 및 마스터 데이터 설정
```bash
# 인덱스 마이그레이션 생성 및 적용
cd NeuroNova_02_back_end/02_django_server
python manage.py makemigrations
python manage.py migrate

# 마스터 데이터 시딩 실행
python manage.py seed_master_data
```

#### 2차: 서비스 기동 및 통합 테스트
```bash
# 백엔드 서비스 시작
cd NeuroNova_02_back_end/07_redis && docker-compose up -d
cd ../05_orthanc_pacs && docker-compose up -d
cd ../02_django_server && python manage.py runserver

# React 클라이언트 (OHIF 포함)
cd NeuroNova_03_front_end_react/00_test_client
npm install
npm start
```

#### 3차: 기능 검증
#### 3차: 기능 검증
- [ ] Django 서버 기동 및 환경 변수 검증 확인 **[FAILED - P-033/P-034]**
- [ ] React 테스트 클라이언트 실행 (Port 3000) **[PASSED - Running on 3001]**
- [ ] **Orthanc 환자 목록 조회** (`/uc05`) **[BLOCKED - Backend Down]**
- [ ] **"View Study" 버튼으로 DICOM Viewer 이동** **[BLOCKED - Backend Down]**
- [ ] **Study 메타데이터 표시 확인** **[BLOCKED - Backend Down]**
- [ ] JWT 토큰 자동 갱신 동작 확인 **[BLOCKED - Backend Down]**
- [ ] N+1 쿼리 런타임 분석 (Django Debug Toolbar) **[BLOCKED - Backend Down]**
- [ ] Foreign Key 마이그레이션 (선택적) **[SKIPPED]**

---
