# CDSS 프로젝트 기술 스택 상세

**작성일**: 2025-12-19
**버전**: 1.0

---

## 📋 목차

1. [Frontend - React Web (TypeScript + Tailwind CSS + Zustand)](#1-frontend---react-web)
2. [Frontend - Flutter Mobile](#2-frontend---flutter-mobile)
3. [Backend - Django](#3-backend---django)
4. [Database - MySQL](#4-database---mysql)
5. [Infrastructure & DevOps](#5-infrastructure--devops)
6. [개발 도구](#6-개발-도구)

---

## 1. Frontend - React Web

### 핵심 기술 스택

#### 언어 및 프레임워크
- **TypeScript 5.x**
  - 엄격한 타입 체크 (`strict: true`)
  - 역할 기반 타입 정의 (`UserRole`, `Permission`)
  - API 응답 타입 안정성
  - 컴포넌트 Props 타입화

- **React 18+**
  - Concurrent Features (Suspense, Transitions)
  - React Hooks (useState, useEffect, useMemo 등)
  - React Router 6 (라우팅)

#### 상태 관리
- **Zustand**
  - 경량 (React Context보다 빠름)
  - 간단한 API (보일러플레이트 최소화)
  - TypeScript 완벽 지원
  - 역할별 스토어 분리:
    ```typescript
    // 예시: stores/authStore.ts
    interface AuthStore {
      user: User | null;
      token: string | null;
      login: (data: LoginRequest) => Promise<void>;
      logout: () => void;
    }
    ```

#### 스타일링
- **Tailwind CSS 3.x**
  - 유틸리티 우선 (Utility-First)
  - JIT 모드 (빠른 빌드)
  - 역할별 커스텀 색상:
    ```javascript
    colors: {
      admin: '#DC2626',    // Red
      doctor: '#2563EB',   // Blue
      rib: '#7C3AED',      // Purple
      lab: '#059669',      // Green
      nurse: '#DB2777',    // Pink
      patient: '#EA580C',  // Orange
      external: '#64748B'  // Gray
    }
    ```
  - 플러그인:
    - `@tailwindcss/forms`: 폼 요소 스타일링
    - `@tailwindcss/typography`: 타이포그래피

#### UI 컴포넌트
- **shadcn/ui**
  - Tailwind 기반 (완전 커스터마이징 가능)
  - Headless UI 기반 (접근성 우수)
  - 복사 붙여넣기 방식 (라이브러리 의존성 없음)
  - 주요 컴포넌트:
    - Button, Input, Select
    - Dialog, DropdownMenu, Popover
    - Table, Card, Badge
    - Toast (알림)

- **Headless UI**
  - 접근성 중심 (ARIA 표준 준수)
  - 키보드 네비게이션 지원
  - 스크린 리더 호환

- **Heroicons**
  - Tailwind 공식 아이콘 세트
  - Solid, Outline 두 가지 스타일

#### 폼 관리
- **React Hook Form**
  - 성능 최적화 (비제어 컴포넌트)
  - 간단한 유효성 검사
  - TypeScript 지원

- **Zod**
  - TypeScript 우선 스키마 검증
  - React Hook Form과 완벽 통합
  - 예시:
    ```typescript
    const loginSchema = z.object({
      username: z.string().min(3, '3자 이상 입력하세요'),
      password: z.string().min(8, '8자 이상 입력하세요'),
    });
    ```

#### HTTP 클라이언트
- **Axios**
  - TypeScript 타입 정의
  - 인터셉터 (토큰 자동 추가, 에러 처리)
  - 요청/응답 변환

#### 실시간 통신
- **native WebSocket**
  - Django Channels 연동
  - 알림 실시간 수신 (UC7)
  - 재연결 로직

#### 데이터 시각화
- **Recharts**
  - React 전용 차트 라이브러리
  - TypeScript 지원
  - 반응형 차트
  - 사용 예:
    - 검사 결과 그래프 (UC4)
    - AI 분석 결과 시각화 (UC6)

#### DICOM 뷰어
- **Cornerstone.js**
  - 의료 영상 전용 라이브러리
  - DICOM 표준 지원
  - Orthanc WADO-RS 연동 (UC5)

#### 날짜/시간
- **date-fns**
  - 경량 (Moment.js 대비 97% 작음)
  - Tree-shaking 지원
  - 타임존 처리
  - 다국어 지원 (한국어)

#### 테스트
- **Vitest**
  - Vite 기반 (빠른 실행)
  - Jest 호환 API
  - TypeScript 지원

- **React Testing Library**
  - 사용자 중심 테스트
  - 역할별 권한 테스트

- **Playwright**
  - E2E 테스트
  - 크로스 브라우저 지원
  - 역할별 사용자 플로우 테스트

---

## 2. Frontend - Flutter Mobile

### 핵심 기술 스택

#### 언어 및 프레임워크
- **Flutter 3.x**
  - Dart 3.x
  - Material Design 3
  - Patient 역할 전용

#### 상태 관리
- **Riverpod**
  - 컴파일 타임 안전성
  - Provider보다 개선된 API
  - 테스트 용이

#### 네트워킹
- **Dio**
  - Flutter용 HTTP 클라이언트
  - 인터셉터 (토큰 관리)
  - 에러 처리

#### 푸시 알림
- **Firebase Cloud Messaging (FCM)**
  - iOS/Android 통합
  - 백그라운드 알림
  - Django와 연동 (UC7)

#### 로컬 저장소
- **SharedPreferences**: 토큰, 설정 저장
- **Hive**: 오프라인 캐싱 (NoSQL)

#### 이미지 처리
- **cached_network_image**: 네트워크 이미지 캐싱
- **flutter_image**: 이미지 최적화

#### 생체 인증
- **local_auth**: 지문, Face ID 인증

---

## 3. Backend - Django

### 핵심 기술 스택

#### 언어 및 프레임워크
- **Python 3.10+**
  - Type Hints 사용
  - Async/Await 지원

- **Django 4.2 LTS**
  - Long Term Support (2026년까지)
  - ORM (PostgreSQL, MySQL 지원)
  - Admin 패널

- **Django REST Framework (DRF)**
  - REST API 구축
  - Serializer (검증 및 직렬화)
  - ViewSet (CRUD)
  - Permission Classes (역할 기반 권한)

#### 인증
- **djangorestframework-simplejwt**
  - JWT Access Token (30분)
  - JWT Refresh Token (7일)
  - Token Rotation

#### 비동기 작업
- **Celery**
  - 백그라운드 작업 처리
  - AI 추론 작업 큐
  - FHIR 동기화 작업 큐
  - 주기적 작업 (Celery Beat)

- **RabbitMQ**
  - 메시지 브로커
  - 큐 구조:
    - `ai.job.create`: AI 추론 요청
    - `fhir.sync`: FHIR 동기화
    - `alert.send`: 실시간 알림

#### 실시간 통신
- **Django Channels**
  - WebSocket 지원
  - Redis Channel Layer
  - 실시간 알림 (UC7)

#### ORM
- **Django ORM**
  - MySQL 8.0 연결
  - 마이그레이션 관리
  - QuerySet 최적화 (`select_related`, `prefetch_related`)

#### 테스트
- **pytest-django**
  - 단위 테스트
  - Fixture 관리
  - 커버리지 측정

#### 모니터링
- **django-prometheus**
  - Prometheus 메트릭 내보내기
  - API RPS, 응답 시간, 에러율

---

## 4. Database - MySQL

### 핵심 기술 스택

- **MySQL 8.0**
  - InnoDB 스토리지 엔진
  - utf8mb4 문자셋
  - 트랜잭션 지원

- **Redis 7.x**
  - 캐시 (Cache-Aside 패턴)
  - Session 저장소
  - Channel Layer (Django Channels)

- **MinIO**
  - S3 호환 객체 스토리지
  - AI Artifact 저장 (MRI, Omics)
  - DICOM 이미지 백업 (선택)

---

## 5. Infrastructure & DevOps

### 핵심 기술 스택

#### 컨테이너화
- **Docker 24+**
  - 멀티 스테이지 빌드
  - 경량 이미지 (Alpine)

- **Docker Compose 2.x**
  - 12개 서비스 오케스트레이션
  - 환경 변수 관리

#### 리버스 프록시
- **Nginx**
  - Load Balancing
  - Rate Limiting (10 req/s)
  - Static File Serving (React Build)
  - SSL/TLS Termination

#### CDN & 보안
- **Cloudflare**
  - CDN (정적 리소스 캐싱)
  - DDoS Protection
  - WAF (SQL Injection, XSS 차단)
  - Universal SSL

#### 모니터링
- **Prometheus 2.x**
  - 시계열 메트릭 수집
  - API RPS, 응답 시간, GPU 사용량

- **Grafana 10.x**
  - 대시보드 시각화
  - System Overview, AI Performance, FHIR Sync

- **Alertmanager 0.26+**
  - Code Blue 알림
  - Slack, Email 통합

#### CI/CD
- **GitHub Actions**
  - 자동 테스트
  - Docker 이미지 빌드
  - 배포 자동화

---

## 6. 개발 도구

### IDE
- **Visual Studio Code**
  - ESLint, Prettier (코드 포매팅)
  - TypeScript 지원
  - Tailwind CSS IntelliSense

- **PyCharm / VS Code**
  - Python 개발
  - Django 지원

### 버전 관리
- **Git**
  - GitHub (코드 호스팅)
  - Conventional Commits (feat:, fix:, docs:)

### API 테스트
- **Postman**
  - API 엔드포인트 테스트
  - 환경 변수 관리

- **Swagger (drf-yasg)**
  - API 자동 문서화
  - 인터랙티브 테스트

### 패키지 관리
- **npm / yarn**: React 패키지 관리
- **pip**: Python 패키지 관리
- **Poetry**: Python 의존성 관리 (선택)

---

## 📦 설치 명령어 요약

### React Web
```bash
# 프로젝트 생성
npx create-react-app cdss-frontend-react --template typescript

# 핵심 라이브러리
npm install axios react-router-dom zustand

# Tailwind CSS
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# UI 컴포넌트
npm install @headlessui/react @heroicons/react
npm install clsx tailwind-merge

# 폼 관리
npm install react-hook-form zod @hookform/resolvers

# 차트 및 날짜
npm install recharts date-fns

# 테스트
npm install -D vitest @testing-library/react @testing-library/jest-dom
npm install -D @playwright/test
```

### Django Backend
```bash
# 가상 환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 핵심 라이브러리
pip install django==4.2
pip install djangorestframework djangorestframework-simplejwt
pip install django-cors-headers
pip install celery redis
pip install channels channels-redis
pip install pymysql
pip install requests

# 모니터링
pip install django-prometheus

# 테스트
pip install pytest pytest-django
```

### Flutter Mobile
```bash
# Flutter 프로젝트 생성
flutter create cdss_patient_app

# 패키지 추가 (pubspec.yaml)
flutter pub add dio
flutter pub add riverpod
flutter pub add shared_preferences
flutter pub add hive hive_flutter
flutter pub add firebase_messaging
flutter pub add local_auth
flutter pub add cached_network_image
```

---

## 🎯 기술 선택 이유

### TypeScript
- **타입 안정성**: 7개 역할, 다양한 권한 체계를 타입으로 명확히 정의
- **개발 생산성**: IDE 자동완성, 컴파일 타임 에러 감지
- **유지보수성**: 대규모 프로젝트에서 리팩토링 안전

### Tailwind CSS
- **빠른 개발**: 유틸리티 클래스로 즉시 스타일 적용
- **일관성**: 역할별 색상, 간격 등 디자인 시스템 통일
- **성능**: JIT 모드로 사용하지 않는 스타일 제거
- **반응형**: 의료진용 데스크톱 최적화 용이

### Zustand
- **경량**: Redux Toolkit보다 간단 (보일러플레이트 최소)
- **성능**: React Context보다 빠름
- **TypeScript**: 완벽한 타입 지원
- **학습 곡선**: 낮음 (빠른 온보딩)

### Django
- **성숙도**: 안정적인 LTS 버전, 풍부한 생태계
- **ORM**: 복잡한 의료 데이터 모델링 용이
- **Admin**: 관리자 패널 즉시 사용
- **DRF**: REST API 빠른 구축

### RabbitMQ
- **안정성**: AMQP 표준, 메시지 보장
- **유연성**: 다양한 큐 패턴 (Work Queue, Pub/Sub)
- **모니터링**: Management UI 제공

---

## ⚠️ 주의사항

### TypeScript
- `strict: true` 모드 사용 (엄격한 타입 체크)
- `any` 타입 지양 (타입 안정성 손실)
- API 응답은 반드시 타입 정의

### Tailwind CSS
- 인라인 스타일 과다 사용 주의 (가독성 저하)
- 복잡한 컴포넌트는 `@apply` 또는 별도 클래스 사용
- Purge 설정 확인 (프로덕션 빌드 크기 최적화)

### Zustand
- 스토어 분리 (역할별, 기능별)
- `get()` 함수로 다른 상태 참조 가능
- 비동기 액션은 try-catch 필수

### Django
- `select_related`, `prefetch_related` 활용 (N+1 쿼리 방지)
- Celery 작업은 멱등성(Idempotent) 보장
- JWT 토큰 만료 시간 적절히 설정 (Access 30분, Refresh 7일)

---

**Last Updated**: 2025-12-19
**Version**: 1.0
**Author**: Claude AI
